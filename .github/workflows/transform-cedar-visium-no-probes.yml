name: Transform Visium (no probes) legacy CEDAR metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/cedar-visium-no-probes/visium-no-probes-field-mappings.csv \
            metadata/cedar-visium-no-probes/visium-no-probes-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/visium-no-probes/latest/visium-no-probes.yml" \
            metadata/cedar-visium-no-probes/visium-no-probes-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/cedar-visium-no-probes/visium-no-probes-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/cedar-visium-no-probes/visium-no-probes-schema.json \
            --patch-file metadata/cedar-visium-no-probes/visium-no-probes-patches.json \
            --input-dir metadata/cedar-visium-no-probes/input \
            --output-dir metadata/cedar-visium-no-probes/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-visium-no-probes/output
          git commit -m "Transform Visium (no probes) legacy CEDAR metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/cedar-visium-no-probes/output \
            metadata/cedar-visium-no-probes/visium-no-probes-schema.json \
            metadata/cedar-visium-no-probes/visium-no-probes-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-visium-no-probes/visium-no-probes-nonstandard-values.json
          git commit -m "Find non-standard values in Visium (no probes) metadata (automated)" || echo "No changes to commit"
          git push

      - name: Commit Excel reports for curator review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-visium-no-probes/todo/
          git commit -m "Generate Excel reports grouped by institution for Visium (no probes) (automated)" || echo "No changes to commit"
          git push
