name: Transform Auto-fluorescense legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/af/af-field-mappings.csv \
            metadata/af/af-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/af/latest/af.yml" \
            metadata/af/af-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/af/af-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/af/af-schema.json \
            --patch-file metadata/af/af-patches.json \
            --input-dir metadata/af/input \
            --output-dir metadata/af/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/af/output
          git commit -m "Transform Auto-fluorescense legacy metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/af/output \
            metadata/af/af-schema.json \
            metadata/af/af-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/af/af-nonstandard-values.json
          git commit -m "Find non-standard values in Auto-fluorescense metadata (automated)" || echo "No changes to commit"
          git push
