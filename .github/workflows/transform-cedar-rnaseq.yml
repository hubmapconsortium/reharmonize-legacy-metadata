name: CEDAR Legacy - Transform RNAseq legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install jinja2
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/cedar-rnaseq/rnaseq-field-mappings.csv \
            metadata/cedar-rnaseq/rnaseq-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/rnaseq/latest/rnaseq.yml" \
            metadata/cedar-rnaseq/rnaseq-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/cedar-rnaseq/rnaseq-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/cedar-rnaseq/rnaseq-schema.json \
            --patch-file metadata/cedar-rnaseq/rnaseq-patches.json \
            --input-dir metadata/cedar-rnaseq/input \
            --output-dir metadata/cedar-rnaseq/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-rnaseq/output
          git commit -m "Transform RNAseq legacy CEDAR metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/cedar-rnaseq/output \
            metadata/cedar-rnaseq/rnaseq-schema.json \
            metadata/cedar-rnaseq/rnaseq-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-rnaseq/rnaseq-nonstandard-values.json
          git commit -m "Find non-standard values in RNAseq metadata (automated)" || echo "No changes to commit"
          git push

      - name: Commit Excel reports for curator review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-rnaseq/todo/
          git commit -m "Generate Excel reports grouped by institution for RNAseq (automated)" || echo "No changes to commit"
          git push

      - name: Generate transformation summary
        run: |
          python scripts/generate-transformation-summary.py \
            --field-mapping-csv metadata/cedar-rnaseq/rnaseq-field-mappings.csv \
            --output-dir metadata/cedar-rnaseq/output \
            --patch-file metadata/cedar-rnaseq/rnaseq-patches.json \
            --title "RNAseq (CEDAR)" \
            --output-file metadata/cedar-rnaseq/transformation-summary.html

      - name: Commit transformation summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-rnaseq/transformation-summary.html
          git commit -m "Generate transformation summary for RNAseq (automated)" || echo "No changes to commit"
          git push
