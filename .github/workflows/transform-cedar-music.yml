name: [CEDAR Legacy] Transform MUSIC legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install jinja2
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/cedar-music/music-field-mappings.csv \
            metadata/cedar-music/music-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/music/latest/music.yml" \
            metadata/cedar-music/music-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/cedar-music/music-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/cedar-music/music-schema.json \
            --patch-file metadata/cedar-music/music-patches.json \
            --input-dir metadata/cedar-music/input \
            --output-dir metadata/cedar-music/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-music/output
          git commit -m "Transform MUSIC legacy CEDAR metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/cedar-music/output \
            metadata/cedar-music/music-schema.json \
            metadata/cedar-music/music-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-music/music-nonstandard-values.json
          git commit -m "Find non-standard values in MUSIC metadata (automated)" || echo "No changes to commit"
          git push

      - name: Commit Excel reports for curator review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-music/todo/
          git commit -m "Generate Excel reports grouped by institution for MUSIC (automated)" || echo "No changes to commit"
          git push

      - name: Generate transformation summary
        run: |
          python scripts/generate-transformation-summary.py \
            --field-mapping-csv metadata/cedar-music/music-field-mappings.csv \
            --output-dir metadata/cedar-music/output \
            --patch-file metadata/cedar-music/music-patches.json \
            --title "MUSIC (CEDAR)" \
            --output-file metadata/cedar-music/transformation-summary.html

      - name: Commit transformation summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-music/transformation-summary.html
          git commit -m "Generate transformation summary for MUSIC (automated)" || echo "No changes to commit"
          git push
