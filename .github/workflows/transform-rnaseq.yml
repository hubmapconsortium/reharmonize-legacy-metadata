name: Transform RNAseq legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install metadata-transformer
        run: |
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/rnaseq/rnaseq-field-mappings.csv \
            metadata/rnaseq/rnaseq-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/rnaseq/latest/rnaseq.yml" \
            metadata/rnaseq/rnaseq-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/rnaseq/rnaseq-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/rnaseq/rnaseq-schema.json \
            --patch-file metadata/rnaseq/rnaseq-patches.json \
            --input-dir metadata/rnaseq/input \
            --output-dir metadata/rnaseq/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/rnaseq/output
          git commit -m "Transform RNAseq legacy metadata (automated)" || echo "No changes to commit"
          git push
