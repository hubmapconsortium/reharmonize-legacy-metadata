name: Transform Cell DIVE legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install jinja2
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/celldive/celldive-field-mappings.csv \
            metadata/celldive/celldive-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/celldive/latest/celldive.yml" \
            metadata/celldive/celldive-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/celldive/celldive-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/celldive/celldive-schema.json \
            --patch-file metadata/celldive/celldive-patches.json \
            --input-dir metadata/celldive/input \
            --output-dir metadata/celldive/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/celldive/output
          git commit -m "Transform Cell DIVE legacy metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/celldive/output \
            metadata/celldive/celldive-schema.json \
            metadata/celldive/celldive-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/celldive/celldive-nonstandard-values.json
          git commit -m "Find non-standard values in Cell DIVE metadata (automated)" || echo "No changes to commit"
          git push

      - name: Commit Excel reports for curator review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/celldive/todo/
          git commit -m "Generate Excel reports grouped by institution for Cell DIVE (automated)" || echo "No changes to commit"
          git push

      - name: Generate transformation summary
        run: |
          python scripts/generate-transformation-summary.py \
            --field-mapping-csv metadata/celldive/celldive-field-mappings.csv \
            --output-dir metadata/celldive/output \
            --patch-file metadata/celldive/celldive-patches.json \
            --title "Cell DIVE" \
            --output-file metadata/celldive/transformation-summary.html

      - name: Commit transformation summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/celldive/transformation-summary.html
          git commit -m "Generate transformation summary for Cell DIVE (automated)" || echo "No changes to commit"
          git push
