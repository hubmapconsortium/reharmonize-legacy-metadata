name: CEDAR Legacy - Transform MALDI legacy metadata

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  transform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML
          pip install openpyxl
          pip install jinja2
          pip install -e tools/json-rules-engine
          pip install -e tools/metadata-transformer

      - name: Generate field mapping
        run: |
          python scripts/generate-field-mapping.py \
            metadata/cedar-maldi/maldi-field-mappings.csv \
            metadata/cedar-maldi/maldi-field-mappings.json

      - name: Generate target schema
        run: |
          python scripts/generate-target-schema.py \
            "https://raw.githubusercontent.com/hubmapconsortium/dataset-metadata-spreadsheet/refs/heads/main/maldi/latest/maldi.yml" \
            metadata/cedar-maldi/maldi-schema.json

      - name: Transform metadata
        run: |
          metadata-transform \
            --field-mapping-file metadata/cedar-maldi/maldi-field-mappings.json \
            --value-mapping-dir shared/value-mappings \
            --target-schema-file metadata/cedar-maldi/maldi-schema.json \
            --patch-file metadata/cedar-maldi/maldi-patches.json \
            --input-dir metadata/cedar-maldi/input \
            --output-dir metadata/cedar-maldi/output

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-maldi/output
          git commit -m "Transform MALDI legacy CEDAR metadata (automated)" || echo "No changes to commit"
          git push

      - name: Find non-standard values
        run: |
          python scripts/find-nonstandard-values.py \
            metadata/cedar-maldi/output \
            metadata/cedar-maldi/maldi-schema.json \
            metadata/cedar-maldi/maldi-nonstandard-values.json

      - name: Commit non-standard values
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-maldi/maldi-nonstandard-values.json
          git commit -m "Find non-standard values in MALDI metadata (automated)" || echo "No changes to commit"
          git push

      - name: Commit Excel reports for curator review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-maldi/todo/
          git commit -m "Generate Excel reports grouped by institution for MALDI (automated)" || echo "No changes to commit"
          git push

      - name: Generate transformation summary
        run: |
          python scripts/generate-transformation-summary.py \
            --field-mapping-csv metadata/cedar-maldi/maldi-field-mappings.csv \
            --output-dir metadata/cedar-maldi/output \
            --patch-file metadata/cedar-maldi/maldi-patches.json \
            --title "MALDI (CEDAR)" \
            --output-file metadata/cedar-maldi/transformation-summary.html

      - name: Commit transformation summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/cedar-maldi/transformation-summary.html
          git commit -m "Generate transformation summary for MALDI (automated)" || echo "No changes to commit"
          git push
